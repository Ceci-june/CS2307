numpy>=1.24
pandas>=2.0
pydantic>=2.0
networkx>=3.0      # layout subgraph cho visualize_graph.py
scikit-learn>=1.2  # eval_engine (recommenders) cần sklearn
# tuỳ chọn cho test:
pytest>=7.0
# embed_cf trong run_eval.py chỉ cần numpy/pandas (đọc embeddings.pkl).
